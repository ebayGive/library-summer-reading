<div class="row">
  <div class="col-md-12"><br/><br><br/><br/><br/><br><br/><br/></div>
</div>

<div class="row">
  <div class="col-md-4"></div>
  <div class="col-md-4">
      <div class="panel panel-primary">
          <div class="panel-heading">
            <h3 class="panel-title">Sign In</h3>
          </div>
          <div class="panel-body">
            <form role="form" method="post" action="/accounts/signin">
              <% if(typeof flash.error() != 'undefined') { %>
                <div class="row">
                    <span class="text text-danger">&nbsp;&nbsp; <%=flash.error()%></span>
                </div>
              <% } %>
              <div class="form-group">
                  <label for="accountName">Account Name</label>
                  <%- contentTag('input', params['accountName'], {name: 'accountName', id: 'accountName', class: 'form-control', placeholder: 'Account Name'});%>
              </div>
              <div class="form-group">
                  <label for="accountName">Passcode</label>
                  <%- contentTag('input', params['passcode'], {name: 'passcode', id: 'passcode', type: 'password', class: 'form-control', placeholder: 'Passcode'});%>
              </div>
              <div class="row">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-primary pull-right">Sign&nbsp;In</button>
                </div>
              </div>
            </form>
          </div>
      </div>
   </div>
  <div class="col-md-4"></div>
</div>
